export class difficultQueryQueue{       // Difficult Query class..This is the node that we append as queue
    OperationType:number;
    constructor(OperationType:number){
      this.OperationType=OperationType;
    }
};